export interface ColumnDisplayConfig {
  displayRemaining?: boolean;
  isCopay?: boolean;
  isDeduct?: boolean;
}

export interface RowConditionConfig {
  isAddRow?: boolean;
  isEditable?: boolean;
  showDelete?: boolean;
}
